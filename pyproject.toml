[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "robotkinematics"
dynamic = ["version"]
description = "抓取任务流水线：把机器人运动学的七个知识点串成七道工序（运动学由 spatialmath + roboticstoolbox 提供）"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
keywords = ["robotics", "kinematics", "SE3", "jacobian", "frank-panda", "education"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Education",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    # 运动学（FK / IK / 雅可比 / 限位）由这两个库提供 —— 本项目不自己实现它们
    "spatialmath-python>=1.1",
    "roboticstoolbox-python>=1.0",
    # numpy<2：roboticstoolbox 在 numpy 2 下会报 _ARRAY_API not found（与课件环境一致）
    "numpy<2",
    "matplotlib>=3.5",
    "PyYAML>=6",
]

[project.optional-dependencies]
dev = ["pytest>=7", "pytest-cov>=4", "ruff>=0.4", "mypy>=1.8"]

[project.scripts]
rkin = "robotkinematics.cli.main:main"

[project.urls]
Documentation = "docs/GUIDE.md"
Source = "https://github.com/BambooRobot/Robot_Kinematics"

[tool.setuptools.packages.find]
where = ["src"]

# 版本号的单一来源是 src/robotkinematics/__init__.py 的 __version__
# （打包时读它，不在两个文件里各写一遍）
[tool.setuptools.dynamic]
version = { attr = "robotkinematics.__version__" }

# ── 测试 ────────────────────────────────────────────────────────────────────
[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q --strict-markers"
markers = [
    "slow: 需要较多计算（例如工作空间采样）的测试",
]

# ── 覆盖率 ──────────────────────────────────────────────────────────────────
[tool.coverage.run]
source = ["src/robotkinematics"]
branch = true
omit = ["*/__main__.py"]

[tool.coverage.report]
show_missing = true
skip_covered = true
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]

# ── 静态检查（ruff：风格 + 常见 bug + import 排序）──────────────────────────
[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src", "tests", "scripts"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle
    "F",    # pyflakes
    "I",    # isort
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "SIM",  # flake8-simplify
    "RUF",  # ruff 自己的规则
]
ignore = [
    "E501",    # 行长由 line-length 控制，且中文注释按显示宽度算会误报
    "SIM108",  # 三元表达式不一定比 if 清楚，本项目优先可读性
    "RUF001",  # 全角标点是有意为之（中文文档字符串）
    "RUF002",
    "RUF003",
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011"]  # 测试里 assert False 是合法写法（这里其实没用，留着以防万一）

[tool.ruff.lint.isort]
known-first-party = ["robotkinematics"]

# ── 类型检查（渐进式：先保证公开接口有类型且互相自洽）───────────────────────
[tool.mypy]
python_version = "3.10"
files = ["src"]
warn_unused_ignores = true
warn_redundant_casts = true
no_implicit_optional = true
strict_equality = true
check_untyped_defs = true
# 可选依赖没装时不该让类型检查失败
ignore_missing_imports = true
exclude = ["^docs/"]
