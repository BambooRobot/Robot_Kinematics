# 数值出处

项目里每个被断言的数值，都能追到出处。三类来源：

| 标记 | 含义 | 可信度 |
|---|---|---|
| **PPT** | 课件里印出来的算例，学生能手算复核 | 最高 |
| **课程日志** | 课件 `run_logs/*.log` 里记录的库输出 | 高 |
| **库** | 由 spatialmath / roboticstoolbox 算出 | 高（就是运动学本身） |
| **自洽** | 本项目自己推导的性质（可达采样、解择优） | 由测试与库对照保证 |

> 验收方式：**自研的可达采样必须与方向相关的物理直觉一致** + **课件 / 库给出的 Panda 数字必须复现**。

---

## 1. 位姿与坐标变换（课件对照）

| 数值 | 来源 | 说明 |
|---|---|---|
| `R = Rz(yaw)·Ry(pitch)·Rx(roll)` ⇔ `SE3.RPY(…, order='zyx')` | **PPT** | 流水线姿态约定 |
| 六元组 (0.4,0.2,0.3,0,0,90°) 下点 (0.1,0,0) → **(0.4, 0.3, 0.3)** | **PPT** | 位姿表示约定 |
| 四元数顺序是 **(w,x,y,z)**（库的约定，与 ROS/Eigen 相反） | **库** | GUIDE 7.2 |
| `^base T_cup.t = [0.7830, 0.1634, 0.8]` | **课程日志** | 默认相机外参下的基准点 |
| `change_x = [0.9562, 0.2634, 0.8]`、`yaw_zero = [0.8, -0.1, 0.8]` | **课程日志** | 变换链敏感性 |

## 2. Franka Panda（全部由库给出）

| 数值 | 来源 | 断言在 |
|---|---|---|
| 7 关节；雅可比形状 **(6, 7)** | **库** | `tests/core/` |
| 关节限位（q4 区间不含 0，所以"全零姿态"真机摆不出） | **库**（`robot.qlim`） | `tests/core/` |
| 零位姿：法兰 **0.926** / 夹爪 TCP **0.8226**（差 103.4 mm） | **库 + 课程日志** | `test_robots.py` |
| 全零姿态是**奇异位形**（σ_min ≈ 1e-16） | **库 + 自洽** | `tests/core/` |
| 雅可比第 7 列线速度为 0（工具挂在第 7 关节轴上） | **库** | `tests/core/` |
| 可操作度：本项目算的（numpy SVD）与库的 `manipulability()` **逐位一致** | **自洽 + 库** | `test_singularity.py` |
| FK→IK 往返位置残差 **< 1e-5**（库的 tol 是关节步长判据，见 GUIDE 7.4） | **库** | `tests/core/` |

## 3. 抓取流水线（自研部分）

| 数值 | 来源 | 断言在 |
|---|---|---|
| 沿"杯子方向"可达 **1.0893 m**，目标 1.1313 m → 余量 **-0.0420 m**（够不着） | **自洽**（采样估计，固定 seed 可复现） | `tests/core/test_workspace.py` |
| 全方向最大可达 **1.19 m** ＞ 沿某方向 **1.09 m** —— 工作空间不是球 | **自洽** | `test_workspace.py` |
| 评分权重：限位余量 0.5 + σ_min 0.3 + 就近 0.2 | **自洽**（写在 `pick.SCORE_WEIGHTS`，可审计） | `tests/usecases/test_pick.py` |
| 七类结论（一类成功 + 六类失败）每类至少一条端到端测试 | **自洽** | `test_pick.py` |

## 4. 换库过程中实测到的库行为（留档）

这些都写进了 `docs/GUIDE.md` 第 7 章，因为它们**只有跑起来才会暴露**：

| 现象 | 实测 |
|---|---|
| `ikine_LM` 的 `tol` 是**关节步长**判据 | 默认值 → 位姿残差 7.85e-4；`tol=1e-9` → 5.22e-7 |
| `robot.reach` | 对本项目模型返回 `0`（int），不可用 |
| `manipulability(method='asada')` | 某些位形返回 `nan` |
| `fkine(batch)` 的返回值 | `isinstance(poses, SE3)` 为 True、`.shape` 报 (4,4) —— 只能用 `.t` 的维数判 |
| `SE3.RPY(…, order='xyz')` vs `'zyx'` | 两套相反的约定；课件里两种都出现过 |
| 法兰 vs 夹爪 TCP | 零位姿 z 差 **103.4 mm**；流水线固定夹爪帧 |
